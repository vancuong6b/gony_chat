name: gony_chat
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.5.1

  # Routing
  go_router: ^14.2.0

  # Database
  sqflite: ^2.4.1
  path: ^1.9.1

  # Google Gemini AI
  google_generative_ai: ^0.4.7

  # Image & Cache
  cached_network_image: ^3.4.1
  image_picker: ^1.1.2

  # UI
  flutter_staggered_grid_view: ^0.7.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Assets (thêm đường dẫn thư mục chứa ảnh, font khi cần)
  # assets:
  #   - assets/images/
  #   - assets/fonts/